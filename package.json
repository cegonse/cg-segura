{
  "name": "cg-segura",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cd dist && cp -r * ../docs && cd .. && rm -rf dist",
    "preview": "vite preview"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vite": "^7.0.7"
  }
}
